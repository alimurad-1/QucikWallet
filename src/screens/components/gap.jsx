import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Gap = ({gap}) => {
  return (
    <View
      style={{
        height: gap,
      }}
    />
  );
};

export default Gap;

const styles = StyleSheet.create({});
